# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# Generated artifacts (rebuilt by pipeline/build_index.py and run_matching.py)
cache/
output/

# Runtime datasets (dataset/trials_clean.csv, dataset/eligibility_criteria_chunks.csv)
# are tracked via Git LFS — see .gitattributes.

# Raw pre-cleaning data — provenance only, not used at runtime; too large to track
dataset/raw/

# Bundled TrialGPT reference repo — has its own git history + large data, not needed at runtime
TrialGPT/

# Internal notes — not for public repo
DONOTPUSH/
