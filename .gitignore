# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# Generated artifacts (rebuilt by pipeline/build_index.py and run_matching.py)
cache/
output/

# Large datasets — exceed GitHub's 100 MB/file limit; distribute separately
dataset/raw/
dataset/trials_clean.csv
dataset/eligibility_criteria_chunks.csv

# Bundled TrialGPT reference repo — has its own git history + large data, not needed at runtime
TrialGPT/
